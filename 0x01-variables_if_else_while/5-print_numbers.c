#include <stdio.h>
/*main - list all numbers between zero and ten
*return:always success
*
*/
int main(void)
{
	int num;
	
	for (num = '0'; num <='10'; num++)
	printf("%d", num);

	putchar('\n');
	return(0);
}
