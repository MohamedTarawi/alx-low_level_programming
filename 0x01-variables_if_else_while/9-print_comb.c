#include <stdio.h> 
/**
* main - prints posible number combinations of single numbers
*
*Return: always 0
*/
int main(void)
{
	int i;

	{
		for (i = 0; i < 10; i++ )
		putchar((i % 10) + '0');
		if (num == 9)
			continue;
		
		putchar(',');
		putchar(' ');
	}
	 putchar('\n');
	 return (0);
} 
