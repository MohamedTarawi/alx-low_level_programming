#include <stdio.h>
/* alphabet in capital and smaal letters
*/
int main (void)
{
	char (low);
	char (up);

	for
	
	       	(low = 'a'; low <= 'z'; up = 'A'; up <= 'Z'; low++)
		putchar (low, up);
		prinf(\n;)

		return (0);
}

